const valid = 'green';
const error = 'red';
const waiting = 'yellow';
const init = 'blue';

const BACKGROUND_COLOR_THEME = Object.freeze({
    valid,
    error,
    waiting,
    init
});

export { BACKGROUND_COLOR_THEME };